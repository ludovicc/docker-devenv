windows:
  - name: "scala"
    clear: true
    options:
      synchronize-panes: true
    root: "~/Projects"
    filters:
      before:
        - "echo 'Let’s use ruby-1.9.3 for each pane in this window.'"
    panes:
      - cmd: /usr/bin/scala
      - cmd:
