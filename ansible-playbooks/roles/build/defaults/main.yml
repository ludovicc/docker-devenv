---

docker_registry: "internal_registry:5000"
images_path: "../images"
docker_build_opts:

external_docker_images:
  - { name: busybox, version: latest }
#  - { name: relateiq/zookeeper, version: latest }
#  - { name: relateiq/kafka, version: latest }
